{
  "name": "boccia-game",
  "private": true,
  "scripts": {
    "build": "node build.cjs",
    "test": "node build.cjs --check && node settings-tests.cjs && node entry-tests.cjs && node timing-tests.cjs"
  }
}
